import { Button } from "@/components/ui/button";
import { openWhatsApp } from "@/lib/whatsapp";

// WhatsAppFloat removed as per request
// WhatsAppFloat removed as per request
const WhatsAppFloat = () => {
  return null;
};

export default WhatsAppFloat;
